<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @company.name %>
</p>
<p>
	<strong>Routes Offered</strong>
</p>
<table>
	<tr>
		<td><strong>From</strong></td>
		<td><strong>To</strong></td>
		<td><strong>Active</strong></td>
	</tr>
<% @company.route_segments.each do |segment| %>
      <tr>
        <td><%= segment.fromLocation.name %></td>
		<td><%= segment.toLocation.name %></td>
        <td><%= segment.active.to_s %></td>
      </tr>
    <% end %>
</table>
<%= link_to 'Edit', edit_company_path(@company) %> |
<%= link_to 'Back', companies_path %>
